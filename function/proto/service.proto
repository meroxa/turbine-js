syntax = "proto3";

package io.meroxa.funtime.proto;

option go_package = "./proto;proto";

message Record {
  string key = 1;
  string value = 2;
  int64 timestamp = 3;
}

service Function {
  rpc Process(Record) returns (Record);
}
